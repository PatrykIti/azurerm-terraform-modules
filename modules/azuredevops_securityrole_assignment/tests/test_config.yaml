# Test configuration for CI/CD integration

test_suites:
  - name: "Unit Tests"
    tests:
      - defaults
    parallel: true
    timeout: 5m

environment:
  required_vars: []

stages:
  - name: "unit"
    description: "Run terraform unit tests"
    test_suites:
      - "Unit Tests"
    fail_fast: true
