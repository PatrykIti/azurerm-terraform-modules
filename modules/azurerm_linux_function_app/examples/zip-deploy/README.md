# Zip Deploy Example

This example demonstrates deploying a ZIP package using `zip_deploy_file`.
Create a `function.zip` in this directory (or update `zip_deploy_file`) before
running `terraform apply`.

<!-- BEGIN_TF_DOCS -->
<!-- END_TF_DOCS -->
